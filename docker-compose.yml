services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: alef_erp_backend
    restart: unless-stopped
    ports:
      - "4002:80"
    environment:
      # These override the .env file inside the container
      DB_CONNECTION: mysql
      DB_HOST: db
      DB_PORT: 3306
      DB_DATABASE: ${DB_DATABASE:-alef_erp}
      DB_USERNAME: ${DB_USERNAME:-root}
      DB_PASSWORD: ${DB_PASSWORD:-root}
      APP_KEY: ${APP_KEY:-}
      APP_ENV: ${APP_ENV:-local}
      APP_DEBUG: ${APP_DEBUG:-true}
      LOG_CHANNEL: ${LOG_CHANNEL:-stderr}
      JWT_SECRET: ${JWT_SECRET:-}
      APP_URL: http://102.211.186.118:4002
      FRONTEND_URL: http://102.211.186.118:5176
    volumes:
      - backend_storage:/var/www/html/storage
      - ./backend/.env:/var/www/html/.env:ro
    depends_on:
      - db
    networks:
      - alef-erp-network

  staff:
    build:
      context: ./staff
      dockerfile: Dockerfile
      args:
        - VITE_API_URL=${VITE_API_URL:-http://102.211.186.118:4002/api}
    container_name: alef_erp_staff
    restart: unless-stopped
    ports:
      - "5176:80"
    depends_on:
      - backend
    networks:
      - alef-erp-network

  member_portal:
    build:
      context: ./member_portal
      dockerfile: Dockerfile
      args:
        - VITE_API_URL=${VITE_API_URL:-http://102.211.186.118:4002/api}
    container_name: alef_erp_member_portal
    restart: unless-stopped
    ports:
      - "7071:80"
    depends_on:
      - backend
    networks:
      - alef-erp-network

  db:
    image: mysql:8.0
    container_name: alef_erp_db
    restart: unless-stopped
    ports:
      - "3309:3306"
    environment:
      MYSQL_DATABASE: ${DB_DATABASE:-alef_erp}
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD:-root}
      # If using root as user, we just set root password.
      # If explicit user needed:
      # MYSQL_USER: ${DB_USERNAME}
      # MYSQL_PASSWORD: ${DB_PASSWORD}
    volumes:
      - db_data:/var/lib/mysql
    networks:
      - alef-erp-network
    command: --default-authentication-plugin=mysql_native_password

  adminer:
    image: adminer
    container_name: alef_erp_adminer
    restart: always
    ports:
      - "8083:8080"
    environment:
      ADMINER_DEFAULT_SERVER: db
    depends_on:
      - db
    networks:
      - alef-erp-network

networks:
  alef-erp-network:
    driver: bridge
    driver_opts:
      com.docker.network.bridge.enable_icc: "true"
    ipam:
      config:
        - subnet: 172.21.0.0/16

volumes:
  db_data:
  backend_storage:
